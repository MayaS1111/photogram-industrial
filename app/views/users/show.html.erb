<h1>
  <%= @user.username %>
</h1>

<%= render "users/profilenav", user: @user%>

<h2>Own photos</h2>
<ul>
  <% @user.own_photos.each do |photo| %>
    <%= render "photos/photo", photo: photo %>
  <% end %>
</ul>
